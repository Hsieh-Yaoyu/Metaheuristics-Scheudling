\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
% The preceding line is only needed to identify funding in the first footnote.
% If that is unneeded, please comment it out.
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{float}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{algpseudocode}
\usepackage{algorithm}

% ====== 新增以下這段來支援中文 ======
\usepackage[AutoFakeBold=true, AutoFakeSlant=true]{xeCJK}
% 設定主要的繁體中文字體，您可以根據您的作業系統或編譯環境更改
\setCJKmainfont{標楷體}   % 如果想要比較正式的襯線字體

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}
\begin{document}

\title{CUDA-Accelerated Implementation of Genetic Algorithm-Optimized Ant Colony Optimization for Pipe Routing \\
{\footnotesize 基於基因演算法最佳化之蟻群演算法於管線佈局問題的 CUDA 加速實現}
}

\author{\IEEEauthorblockN{1\textsuperscript{st} Hung Ming-Chun}
\IEEEauthorblockA{\textit{Department of Computer Science and} \\
\textit{Information Engineering}\\
\textit{National Taiwan Normal University}\\
Taipei, Taiwan(R.O.C) \\
61447026s@ntnu.edu.tw}
\and
\IEEEauthorblockN{2\textsuperscript{nd} Hsieh Yao-yu}
\IEEEauthorblockA{\textit{Graduate Institute of} \\
\textit{Science Education}\\
\textit{National Taiwan Normal University}\\
Taipei, Taiwan(R.O.C) \\
61345001s@ntnu.edu.tw}
}

\maketitle

\begin{abstract}
管線佈局（Pipe Routing）問題在現代工程設計中極具挑戰性，
傳統路徑演算法在處理多管線避讓時往往面臨運算瓶頸。
蟻群演算法（ACO）在求解此類空間尋優問題上極具潛力，但其效能高度依賴人工設定之超參數，
設定不當極易導致早熟收斂或陷入局部最佳解。有鑑於此，
本研究提出一種結合基因演算法（GA）與蟻群演算法之混合最佳化架構（GA-ACO），
利用 GA 自動搜尋 ACO 之最佳超參數組合（包含費洛蒙權重、啟發式權重、轉彎懲罰與擴散半徑等），
並引入軟性懲罰機制以解決多管線間的安全距離衝突。為克服 GA-ACO 龐大的計算負荷，
本研究利用 CUDA 統一計算架構實現了深度的多執行緒平行化，
大幅縮短演算法之評估時間。實驗結果顯示，在五組不同複雜度的網格地圖測試中，
GA-ACO 架構相較於傳統固定參數之 ACO，其平均路徑成本最高可獲得 80.7\%
 的顯著改善。此外，透過多次獨立實驗證明，GA-ACO 不僅能有效降低路徑成本，
 更大幅縮小了結果的變異程度，展現出卓越的收斂穩定性與強健性，
 為複雜管線佈局問題提供了一套高效且實用的解決方案。

\end{abstract}
\section{Introduction}
隨著現代工程與建築設計的複雜度日益提升，
管線佈局（Pipe Routing）問題已成為設施配置中不可或缺且極具挑戰性的一環。
在有限的空間內，如何有效且安全地佈置多種不同屬性的管線（如瓦斯管、電管等），
同時滿足物理限制與工程規範，是一個典型的 NP-hard 最佳化問題 \cite{b1}。
傳統的佈局方法多依賴工程師的經驗，不僅耗時費力，且難以在複雜環境中保證解的最佳性。\\

為了解決此問題，啟發式演算法（Metaheuristics）逐漸被引入管線佈局的領域。
其中，蟻群演算法（Ant Colony Optimization, ACO）因其優異的正向回饋機制與探索路徑能力，
在陳俊仁與翁世光教授的研究中已展現出解決空間尋優問題的潛力 \cite{b2}。
然而，ACO 演算法的效能高度依賴於超參數（如費洛蒙權重 $\alpha$、啟發式權重 $\beta$ 等）的設定。
初步的探索性測試表明，超參數的微小擾動（例如 $\alpha$ 值僅改變 0.1）
便可能導致演算法走向完全不同的收斂路徑與解的品質。這種高度的參數敏感度與人工調參的盲目性，
極易導致演算法陷入局部最佳解或收斂過於緩慢，突顯了自動化參數調校的迫切需求。\\

有鑑於此，本研究提出一種混合式的最佳化架構（GA-ACO），
引入基因演算法（Genetic Algorithm, GA）作為高階參數調校機制，
動態且自動地搜尋 ACO 的最佳參數組合。本研究之主要貢獻可歸納為以下四點：
\begin{itemize}
    \item \textbf{混合演算法架構}：成功結合 GA 與 ACO，利用 GA 的全域搜尋能力克服 ACO 參數敏感的缺陷，
    實現了超參數的自動化調校與最佳化。
    \item \textbf{CUDA 雙重平行化加速}：針對多管線佈局與 GA 評估的龐大計算負荷，
    利用 CUDA 架構（GPU Streams）對 ACO 的路徑探索進行了「基因」與「螞蟻」層級的大規模平行化，大幅縮短運算時間。
    \item \textbf{軟性懲罰避讓機制}：針對多屬性管線（瓦斯與電），
    設計了結合轉彎成本與安全距離的軟性懲罰函數（Soft Penalty），
    確保管線在受限空間中的合法性與合理性。
    \item \textbf{動態視覺化監控系統}：開發基於 OpenCV 與 Gnuplot 的即時動態視覺化系統，
    能直觀展示管線收斂結果與 GA 演化軌跡，提供高擴展性的實驗平台。
\end{itemize}

本論文後續章節安排如下：第二章將回顧管線佈局與相關演算法之文獻；第三章詳細探討 GA-ACO 架構、CUDA 加速設計與視覺化實作細節；第四章將展示並分析各項實驗數據與效能評估；最後於第五章總結本研究並提出未來展望。

\section{Related Work}

\subsection{Pipe Routing Problem}
管線佈局問題廣泛存在於船舶設計、化工廠、建築機電（MEP）與積體電路設計等領域 \cite{b1}。
其核心目標是在給定的三維或二維空間中，為多個起點與終點尋找無碰撞的連接路徑，
同時將總長度、彎折次數及與其他管線的安全距離最小化。早期的研究多採用確定性演算法，
如 Dijkstra 演算法 \cite{b3} 或 A* 演算法 \cite{b4}，雖然能確保找到單一管線的最短路徑，
但在處理多管線且需考慮複雜避讓規則時，其運算時間會隨問題規模呈指數增長。
近年來，研究焦點逐漸轉向啟發式演算法，期待合理時間內求得近似最佳解。

\subsection{Genetic Algorithm}
基因演算法（Genetic Algorithm, GA）是一種模擬自然界物種演化過程的群體演算法 \cite{b5}。
透過選擇（Selection）、交配（Crossover）與突變（Mutation）等操作，
GA 能夠在廣大的搜尋空間中進行全域搜索。在工程最佳化問題中，
GA 常被用來解決離散或連續變數的組合問題。在本研究中，GA 並非直接用於規劃管線路徑，
而是被應用於超參數最佳化（Hyperparameter Tuning），
藉由評估不同參數組合（如 $\alpha$, $\beta$, $\rho$, $Q$ 等）所產生的路徑品質，
引導整體架構朝向更穩健的設定演化，克服人工調參的盲目性與耗時性。

\subsection{Ant Colony Optimization}
蟻群演算法（Ant Colony Optimization, ACO）是由 Marco Dorigo 等人提出 \cite{b6}，
模擬自然界螞蟻覓食行為的演算法。螞蟻在行走路徑上會留下費洛蒙（Pheromone），
後續的螞蟻會根據費洛蒙濃度與啟發式資訊選擇路徑，進而促使整個蟻群逐漸收斂至最短路徑。
在管線佈局應用中，ACO 能夠輕易整合各種空間限制與工程懲罰（如轉彎成本），
具備良好的空間適應能力，我們參考陳俊仁與翁世光教授的研究 \cite{b2}，
但為了時間與複雜度我們決定先將管先設置為兩個相互排斥管線以及降至為二維空間，
同時考量到現實生活中管線多半為單一進入點與多個終點的佈局需求(如室內配電為電箱為起點，電器為終點)，
因此我們只針對單一起點與多終點的佈局進行優化
。然而，傳統 ACO 面臨著容易早熟收斂（Premature Convergence）
與參數敏感度高的缺陷，這也是本研究結合 GA 進行優化的主要動機。

\subsection{CUDA Acceleration}
統一計算架構（Compute Unified Device Architecture, CUDA）是 NVIDIA 推出的平行運算平台與編程模型，
允許開發者利用圖形處理器（GPU）強大的多核心運算能力來處理通用計算任務。在群體智慧演算法中，個體
（如螞蟻或染色體）之間的行為多具備高度的獨立性，非常適合映射至 GPU 的平行執行緒（Threads）上。
近年相關文獻指出，將 ACO 移植至 CUDA 架構可大幅縮短演算法的執行時間 \cite{b7}。
本研究進一步利用 CUDA Streams 技術，實現了「基因層級」與「螞蟻層級」的雙重平行化，
極大地提升了 GA-ACO 混合演算法在複雜環境下的執行效率。

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Implement Details}

\subsection{Genetic Algorithm}
本研究的 GA 設計旨在為 ACO 尋找最佳的超參數組合。
我們將 ACO 的關鍵參數定義為染色體（Chromosome），
包含費洛蒙權重 $\alpha$、啟發式權重 $\beta$、費洛蒙揮發率 $\rho$、釋放常數 $Q$、
轉彎懲罰權重 $turn\_w$、安全距離懲罰權重 $clear\_w$ 以及擴散半徑 $diffusion\_rad$。
演化過程如下：首先隨機初始化一個大小為 $N$ 的族群，並為了避免「冷啟動死亡（Cold Start Failure）」，
在初始族群中植入一組已知的保底參數。每一代的評估階段，GA 會呼叫 ACO 
進行模擬並計算適應度（Fitness），適應度定義為該組參數所產生之最佳管線組合的總成本
（包含路徑長度、轉彎懲罰與安全距離軟性懲罰）。接著，利用排序法保留前 50\% 的菁英個體，
並對剩餘個體進行算術交配與 20\% 機率的隨機突變，以產生下一代子代。

\subsubsection{Crossover Function and Mutation}
在子代的生成過程中，本研究採用算術交配（Arithmetic Crossover）與均勻突變（Uniform Mutation）。
假設從菁英群體中透過輪盤或隨機挑選出兩組父代個體 $P_1$ 與 $P_2$，其產生的子代 $C$ 基因計算方式為兩者之算術平均：
\begin{equation}
C_{gene} = \frac{P_{1,gene} + P_{2,gene}}{2}
\end{equation}
為維持族群的多樣性並避免陷入局部最佳解，每個生成的子代基因皆有 $p_{m} = 0.2$ 的機率發生突變。突變操作會無視父代特徵，直接於該參數的合理物理邊界 $[L_{bound}, U_{bound}]$ 內，重新生成一均勻分布之隨機數。

\begin{algorithm}[!t]
\caption{Genetic Algorithm for ACO Hyperparameter Tuning}
\begin{algorithmic}[1]
\STATE Initialize Population $P$ with size $N$
\STATE Insert known robust parameters into $P[0]$ (Seeding)
\FOR{$gen = 1$ to $GA\_GENERATIONS$}
    \STATE \textbf{Parallel Evaluate:} $Fitness(P_{i}) \leftarrow \text{run\_aco}(P_{i}) \quad \forall i \in \{1..N\}$
    \STATE Sort $P$ in ascending order based on $Fitness$
    \STATE Keep top 50\% of $P$ as Elites
    \FOR{$i = N/2$ to $N-1$}
        \STATE Select Parents $P_a, P_b$ from Elites randomly
        \STATE $P_{i} \leftarrow \text{Crossover}(P_a, P_b)$
        \IF{$rand() < 0.2$}
            \STATE $P_{i} \leftarrow \text{Mutate}(P_{i})$
            \STATE $P_{i}.Fitness \leftarrow -1$ (Require re-evaluation)
        \ENDIF
    \ENDFOR
\ENDFOR
\STATE \textbf{Return} Best Chromosome $P[0]$
\end{algorithmic}
\end{algorithm}


\subsection{Ant Colony Optimization}
在 ACO 的實作中，我們將每種屬性的管線（如瓦斯管與電管）指派給特定類型的螞蟻。
為了支援多終點的佈局，螞蟻的生成採用餘數與商數的交錯分配法，確保所有起迄組合均被均勻探索。
螞蟻的移動決策基於輪盤賭選擇法（Roulette Wheel Selection），
並引入了 $\epsilon$-Greedy 策略，賦予螞蟻 3\% 的機率進行盲目隨機探索，
以利跳脫局部最佳解。在費洛蒙更新機制上，我們採用了混合釋放策略：
不僅該回合的區域菁英（Iteration Best）會釋放費洛蒙以維持探索動能，
全域歷史最佳解（Global Best）亦會釋放大量費洛蒙以強化主幹道的收斂。
此外，當不同管線發生重疊或違反安全距離（Safe Distance）時，
系統會計算重疊次數並給予極重的軟性懲罰，引導演算法尋找無碰撞的完美配置。

\subsubsection{Cost Function}
為了全面評估管線配置的優劣，相比陳俊仁與翁世光教授的研究 \cite{b2}只使用路徑長度和管線彎折作為成本函數（Cost Function），
本研究設計之成本函數另外加入管線間的實體干涉懲罰，可以使螞蟻間的路徑選擇更具現實意義。
單一螞蟻的路徑成本 $S_{path}$ 定義如下：
\begin{equation}
S_{path} = L + (W_{turn} \times C_{turn}) + (W_{clear} \times P_{clear})
\end{equation}
其中，$L$ 為路徑網格總長度，$C_{turn}$ 為路徑彎折次數，$P_{clear}$ 為進入其他管線安全半徑內的嚴重程度累加值；$W_{turn}$ 與 $W_{clear}$ 則為受 GA 調控的基因權重。
而在全域聯合評估時，若瓦斯管與電管發生實體重疊，我們不採用硬性淘汰（Hard Constraint），而是給予軟性梯度懲罰（Soft Penalty）：
\begin{equation}
S_{global} = \sum S_{path} + (1000 \times C_{overlap})
\end{equation}
此機制允許 GA 在初期接受包含輕微重疊的瑕疵解，並透過梯度引導族群逐步收斂至重疊數 $C_{overlap} = 0$ 的合法佈局。

\begin{algorithm}[!ht]
\caption{Parallel ACO Routing with Soft Penalty}
\begin{algorithmic}[1]
\FOR{$iter = 1$ to $MAX\_ITER$}
    \STATE Move all ants concurrently in GPU Kernel
    \STATE Evaporate Pheromones: $\tau \leftarrow \tau \times (1 - \rho)$
    \FOR{each pipe type and endpoint}
        \STATE Evaluate $S_{path}$ using Eq (2)
        \STATE Update Iteration Best ant
    \ENDFOR
    \IF{all endpoints reached}
        \STATE Calculate $C_{overlap}$ between different pipe types
        \STATE Evaluate combined score $S_{global}$ using Eq (3)
        \IF{$S_{global} < Best\_S_{global}$}
            \STATE Update Global Best Paths
        \ENDIF
    \ENDIF
    \STATE Deposit Pheromones (Iteration Best $\times 30\%$ + Global Best $\times 70\%$)
\ENDFOR
\end{algorithmic}
\end{algorithm}


\subsection{CUDA Acceleration}
為了解決 GA-ACO 架構龐大的計算需求，本研究在 GPU 端進行了深度的平行化封裝。
我們設計了 \texttt{ACO\_Environment} 類別，
為 GA 族群中的每一個染色體配置獨立的 GPU 記憶體區塊（包含費洛蒙矩陣、螞蟻狀態與隨機數種子）
以及專屬的並行通道（\texttt{cudaStream\_t}）。
在 CPU 端，利用 C++ 的 \texttt{std::thread} 同時喚醒所有染色體的評估程序；
而在 GPU 端，各染色體的 ACO 模擬被分配至不同的 Stream 異步執行（Asynchronous Execution）。
此設計確保了十組甚至數十組 ACO 演算法能夠在 GPU 內部「真正同時」運行。

\begin{algorithm}[!t]
\caption{Multi-Stream CUDA Execution Architecture}
\begin{algorithmic}[1]
\STATE Array \texttt{envs}[$N$] of \texttt{ACO\_Environment} (each with unique \texttt{cudaStream\_t})
\STATE Array \texttt{threads}[$N$]
\FOR{$i = 1$ to $N$ in CPU}
    \STATE \texttt{threads}[$i$] $\leftarrow$ spawn thread to run \texttt{envs}[$i$].run\_aco($P_i$)
\ENDFOR
\STATE // Inside \texttt{run\_aco} function for environment $i$:
\STATE \texttt{cudaMemcpyAsync}(..., \texttt{envs}[$i$].stream)
\STATE \texttt{ant\_movement\_kernel}$<<<blocks, threads, 0, \text{envs}[i].stream>>>$(...)
\STATE \texttt{cudaMemcpyAsync}(..., \texttt{envs}[$i$].stream)
\STATE \texttt{cudaStreamSynchronize}(\texttt{envs}[$i$].stream)
\STATE Join all \texttt{threads}
\end{algorithmic}
\end{algorithm}


\subsection{Visualizer}
為便於分析與展示演算法成果，本研究開發了整合 OpenCV 與 Gnuplot 的視覺化模組。
在 ACO 的尋優過程中，OpenCV 負責將二維網格地圖渲染為圖形介面，
即時顯示各管線的費洛蒙分佈（以不同顏色的透明度疊加）以及全域最佳路徑的骨架中心線。
當單次 ACO 執行完畢時，系統會自動將最終成果截圖保存。\\
同時，在 GA 演化的巨觀層面，系統利用背景執行續呼叫 Gnuplot，
並將每一代的最佳適應度（Best Score）與中位數適應度（Median Score）即時寫入 Log 檔進行繪圖。
此機制提供了一目了然的演化收斂折線圖，協助開發者即時監控參數優化狀態，
並確保了無頭模式（Headless）下執行與輸出的穩定性。
\subsection{Visualizer}
為便於分析與展示演算法成果，本研究開發了整合 OpenCV 與 Gnuplot 的視覺化模組。
在 ACO 的尋優過程中，OpenCV 負責將二維網格地圖渲染為圖形介面，
即時顯示各管線的費洛蒙分佈（以不同顏色的透明度疊加）以及全域最佳路徑的骨架中心線。

為了讓使用者能直觀地監控演算法狀態，視覺化介面的設計如圖 \ref{fig:Vis_explain} 所示。
\begin{figure}[!ht]
\centering
\includegraphics[width=0.45\textwidth]{images/Vis_explain.png}
\caption{GA-ACO 演算法視覺化介面與標示說明}
\label{fig:Vis_explain}
\end{figure}

在圖 \ref{fig:Vis_explain} 中，介面左上角顯示了當前演算法的執行進度與參數狀態。
第一行粗體字由左至右分別為：當前 GA 世代（Generation）、當前 ACO 迭代（Iteration）以及該迭代的全域最佳適應度（Best Score）。
其下方的細小字體則為當前評估中之超參數組合的詳細數值，包含 $\alpha$、$\beta$、$\rho$、$Q$、$turn\_w$、$clear\_w$ 與 $diffusion\_rad$。
在圖像的主體路由部分，黃色實體區塊表示瓦斯管徑，藍色為電管；帶有粗框的方格表示當前費洛蒙濃度較高的熱區，顏色的深淺則對應費洛蒙濃度的強弱。此外，貫穿其中的深色粗線代表當前全域最佳解（Global Best）的路徑骨架。當單次 ACO 執行完畢時，系統會自動將最終成果截圖保存。\\

同時，在 GA 演化的巨觀層面，系統利用背景執行緒呼叫 Gnuplot，
並將每一代的最佳適應度（Best Score）與中位數適應度（Median Score）即時寫入 Log 檔進行繪圖。
此機制提供了一目了然的演化收斂折線圖，協助開發者即時監控參數優化狀態，
並確保了無頭模式（Headless）下執行與輸出的穩定性。

\begin{algorithm}[!t]
\caption{Background Visualization Workflow}
\begin{algorithmic}[1]
\STATE Initialize headless Gnuplot process via \texttt{popen}
\FOR{each GA Generation}
    \STATE Calculate $Best\_Score$ and $Median\_Score$
    \STATE Append scores to \texttt{ga\_log.txt}
    \STATE Send \texttt{plot} commands to Gnuplot pipe
    \STATE Save Gnuplot output as \texttt{ga\_progress.png}
    \STATE OpenCV \texttt{imread} and \texttt{imshow} the chart
    \STATE Render best ACO simulation to OpenCV Mat
    \IF{generation == final}
        \STATE \texttt{imwrite} final ACO path grid to file
    \ENDIF
\ENDFOR
\end{algorithmic}
\end{algorithm}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Experiment}

\subsection{Motivation: Observation of Parameter Sensitivity}
為了具體說明傳統 ACO 面臨的參數敏感度困境，我們在進行大規模評估前，先進行了一組探索性測試。
如圖 \ref{fig:motivation_alpha} 所示，我們僅針對單一超參數（費洛蒙權重 $\alpha$）進行微調，其餘參數保持完全一致。

\begin{figure}[!ht]
    \centering
    \begin{subfigure}[b]{0.48\columnwidth}
        \centering
        \includegraphics[width=\textwidth]{images/intro_01.png}
        \caption{$\alpha=1.5$ (Score: 344.2)}
        \label{fig:aco_parameter_01}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\columnwidth}
        \centering
        \includegraphics[width=\textwidth]{images/intro_02.png}
        \caption{$\alpha=1.4$ (Score: 308.4)}
        \label{fig:aco_parameter_02}
    \end{subfigure}
    \caption{ACO 參數敏感度觀察：微小的 $\alpha$ 變動導致完全不同的收斂路徑}
    \label{fig:motivation_alpha}
\end{figure}

觀察圖 \ref{fig:motivation_alpha} 可發現，當 $\alpha=1.5$ 時
（圖 \ref{fig:motivation_alpha}a），電管（藍線）選擇從右側繞行；
然而當 $\alpha$ 僅僅微調 0.1 降至 1.4 時（圖 \ref{fig:motivation_alpha}b），
電管卻直接貫穿中央通道，且最終適應度（Score）發生了顯著變化。
此現象實證了 ACO 在複雜避讓問題中的高度不穩定性，若依賴人工經驗進行網格搜索（Grid Search），
極難在龐大的高維連續空間中找到全域最佳解。
此觀察結果也確立了本研究導入 GA 進行自動化參數最佳化的絕對必要性。

% === 新增：實驗環境與測試資料集 ===
\subsection{Experimental Setup and Datasets}
為了全面評估本研究所提出的 GA-ACO 混合演算法之效能與泛化能力，我們設計了五組不同複雜度的二維網格地圖（Map 0 至 Map 4）進行測試。
每張地圖的尺寸設定為 $18 \times 24$ 網格，並包含不同型態的障礙物分佈與空間限制。
在測試案例中，包含了瓦斯管（黃色）與電管（藍色）兩種互相排斥的管線佈局需求，皆採用單一起點對應多終點（Multi-endpoint）的配置。

五組地圖的具體配置與 GA-ACO 的最終最佳化路由結果如圖 \ref{fig:all_maps} 所示。各別地圖之特徵設計如下：
\begin{itemize}
    \item \textbf{Map 0}：基礎對稱地形，具備寬敞的中央通道，用以測試演算法的基礎尋路與避讓能力。
    \item \textbf{Map 1}：存在大量不規則的封閉區塊與對角線交叉路徑，管線極易發生死結，嚴格考驗演算法在複雜干涉下的整體避讓機制。
    \item \textbf{Map 2}：具備極端的橫向狹長通道，強迫兩種管線在受限的單一通道內外競爭有限的無障礙空間。
    \item \textbf{Map 3}：中央具有半封閉的「密室房間」結構，考驗螞蟻進入死胡同後的脫困與多終點收斂探索能力。
    \item \textbf{Map 4}：高度刁鑽的迷宮型地圖，存在多個被迫交會的窄巷，嚴格測試軟性懲罰機制（Soft Penalty）與擴散半徑（Diffusion Radius）在極限環境下的參數收斂極限。
\end{itemize}

\begin{figure*}[!ht]
    \centering
    % 第一排：Map 0, Map 1, Map 2
    \begin{subfigure}[b]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/map0.png}
        \caption{Map 0}
        \label{fig:map0}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/map1.png}
        \caption{Map 1}
        \label{fig:map1}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/map2.png}
        \caption{Map 2}
        \label{fig:map2}
    \end{subfigure}
    
    \vskip\baselineskip % 換行並增加間距
    
    % 第二排：Map 3, Map 4 (置中對齊)
    \begin{subfigure}[b]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/map3.png}
        \caption{Map 3}
        \label{fig:map3}
    \end{subfigure}
    \hspace{0.05\textwidth} % 給予中間一點間距讓兩張圖置中對齊更好看
    \begin{subfigure}[b]{0.32\textwidth}
        \centering
        \includegraphics[width=\textwidth]{images/map4.png}
        \caption{Map 4}
        \label{fig:map4}
    \end{subfigure}
    \caption{實驗採用之五組二維管線佈局測試地圖與 GA-ACO 最終最佳化結果}
    \label{fig:all_maps}
\end{figure*}
% ==============================

\subsection{CUDA Acceleration Performance}
為了驗證 CUDA 加速架構在 GA-ACO 演算法中的實際效益，本研究針對 CPU 與 GPU 的執行時間進行了對照實驗。
為體現 CUDA 強大的平行運算能力，同時避免過多的 PCIe 記憶體傳輸開銷佔據主導地位，
實驗統一將螞蟻數量設定為 $2000$ 隻，並將 GA 的族群大小定為 $N=40$。
硬體環境採用 NVIDIA RTX 3090 GPU 作為硬體加速器，
並以具備 14 核心 28 執行緒的伺服器級處理器 Intel Xeon E5-2680 v4 CPU 作為對照組
（啟用 OpenMP 多核心加速），作業系統為 Ubuntu 24.04。

實驗紀錄了前 3 次 GA 迭代（Iteration）的執行時間，詳細數據如表 \ref{tab:cuda_perf} 所示。

\begin{table}[!t]
    \centering
    \caption{CPU 與 GPU 在不同迭代次數下的執行時間比較}
    \label{tab:cuda_perf}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{|c|c|c|}
        \hline
        \textbf{Iteration} & \textbf{GPU Time} & \textbf{CPU Time} \\ \hline
        1 & 123.621 s & 103.348 s \\ \hline
        2 & 62.7254 s & 64.8276 s \\ \hline
        3 & 60.0447 s & 67.7726 s \\ \hline
    \end{tabular}
\end{table}

由表 \ref{tab:cuda_perf} 的實驗結果可觀察到一項經典的異質運算現象：
在第一次迭代（Iteration 1）中，GPU 的執行時間（123.621 秒）反倒略慢於 CPU（103.348 秒）。
此現象主要歸因於 CUDA 架構初次啟動時的「初始化開銷（Initialization Overhead）」。
在程式剛啟動時，系統必須進行 GPU 端的記憶體配置（如 \texttt{cudaMalloc}）、cuRAND 
隨機數生成器狀態矩陣的建立，以及大量的初始地圖與環境參數從主機端（Host）複製至設備端（Device）等操作，
這些硬體層級的準備工作導致了首代的耗時較長。

然而，一旦初始化完成並進入穩定運算階段（Iteration 2 與 Iteration 3），
GPU 的極致平行優勢便立刻顯現。從第二次迭代開始，GPU 的執行時間穩定下降至約 60 秒至 62 秒，
成功反超並領先 CPU 的 64 秒至 67 秒。考量到本實驗每代需同時評估 40 組參數，
相當於每代有高達 $80,000$ 隻螞蟻在進行路徑探索，Intel Xeon E5-2680 v4
 憑藉其強大的多執行緒能力維持了相當出色的基準表現；但 RTX 3090 依舊藉由其數以千計的 CUDA 核心，
 在常態運算中展現了更卓越的吞吐量。可以預期，未來若進一步擴展網格地圖的規模或增加螞蟻群體的數量，
 GPU 分攤掉記憶體傳輸成本後的加速比（Speedup Ratio）將會更加顯著。
 本架構具備高度彈性，當螞蟻數量龐大時（如 2000 隻），CUDA 能提供顯著加速；
 但在超參數最佳化階段，由於我們採取了較小的個體規模（Ant=100），
 為了避免 PCIe 記憶體傳輸成為瓶頸，系統能自適應切換至 CPU OpenMP 進行極速評估，
 展現了本架構在不同運算規模下的強健性。

\subsection{GA-ACO Performance}

\subsubsection{Convergence Analysis of Genetic Optimization}

為了驗證分析基因演算法於超參數搜尋過程中的收斂行為，以驗證其最佳化流程之有效性。圖 \ref{fig:ga_convergence_01} 至圖 \ref{fig:ga_convergence_4} 顯示五組測試地圖於 20 次獨立實驗下，各世代最佳個體之 Score 分布以及平均收斂趨勢。

\begin{figure}[!t]
\centering
\includegraphics[width=1.0\columnwidth]{data/boxplot_map0_converge.png}
\includegraphics[width=1.0\columnwidth]{data/boxplot_map1_converge.png}
\caption{GA convergence behavior on Map0 and Map1 over 25 generations.}
\label{fig:ga_convergence_01}
\end{figure}

由圖 \ref{fig:ga_convergence_01} 可觀察到，Map0 與 Map1 的平均 Score 皆隨著世代增加而持續下降。其中 Map0 的平均 Score 約由 295 降至 155，而 Map1 則由約 97,000 降至 45,000。雖然 Map1 的搜尋空間較大且結果變異較高，但其收斂曲線仍呈現穩定下降趨勢，顯示 GA 能有效改善 ACO 的參數設定。

\begin{figure}[!t]
\centering
\includegraphics[width=1.0\columnwidth]{data/boxplot_map2_converge.png}
\includegraphics[width=1.0\columnwidth]{data/boxplot_map3_converge.png}
\caption{GA convergence behavior on Map2 and Map3 over 25 generations.}
\label{fig:ga_convergence_23}
\end{figure}

由圖 \ref{fig:ga_convergence_23} 可觀察到，Map2 與 Map3 同樣具有明顯的收斂現象。Map2 的平均 Score 約由 145 降至 64，而 Map3 則由約 240 降至 158。除了平均值持續下降之外，盒鬚圖的分布範圍亦逐漸縮小，代表族群內個體逐漸集中於相似的高品質參數組合，顯示基因演算法已逐步收斂至穩定解附近。

\begin{figure}[!t]
\centering
\includegraphics[width=1.0\columnwidth]{data/boxplot_map4_converge.png}
\caption{GA convergence behavior on Map4 over 25 generations.}
\label{fig:ga_convergence_4}
\end{figure}

由圖 \ref{fig:ga_convergence_4} 可觀察到，Map4 的平均 Score 約由 295 降至 155，下降幅度接近 50\%。與其他測試地圖相同，大部分改善皆發生於前 10 個世代，而在第 15 至 20 個世代後逐漸趨於平穩，顯示演化過程已接近收斂狀態。

綜合上述結果可知，本研究所提出之 GA-ACO 架構具有良好的收斂特性。無論在簡單或複雜地圖中，基因演算法皆能於有限世代內搜尋出較佳的 ACO 超參數組合，並逐步收斂至穩定解。平均 Score 的持續下降以及族群變異的逐步縮減，皆證明本研究之參數最佳化流程具有良好的搜尋能力與穩定性，能有效提升後續管線佈局問題之求解品質。



\subsubsection{Hyperparameter Analysis and Verification}

在此章節中，我們將展示 GA-ACO 演算法在管線佈局問題上的優化效果。為了驗證 GA 在 ACO 參數調校上的有效性，我們設計了一組對照實驗：一組為固定參數 ACO，即使用預設的參數組合（如 $\alpha=1.0$、$\beta=2.0$、$\rho=0.5$ 等）進行多次獨立執行；另一組為 GA-ACO，即讓 GA 自動搜尋最佳參數組合並評估其性能。

以下實驗我們使用 map0 作為測試場景，該地圖包含兩條相互排斥的管線（瓦斯管與電管），並且每條管線均有一個起點與多個終點的佈局需求。對照組採用固定參數 ACO，實驗組則使用 GA 所搜尋到的最佳參數組合，如表 \ref{tab:aco_params} 所示。

\begin{table}[!t]
\caption{Parameter Settings of ACO and GA-ACO}
\label{tab:aco_params}
\centering
\begin{tabular}{c c c}
\hline
Parameter & ACO & GA-ACO \\
\hline
$\alpha$ & 1.00 & 1.68044 \\
$\beta$ & 2.00 & 3.99775 \\
$\rho$ & 0.50 & 0.177945 \\
$Q$ & 100.00 & 106.823 \\
$turn\_w$ & 1.00 & 1.63136 \\
$clear\_w$ & 1.00 & 2.67383 \\
$diffusion\_rad$ & 2.00 & 1.27466 \\
\hline
\end{tabular}
\end{table}

由表 \ref{tab:aco_params} 可觀察到，GA 所搜尋出的參數與固定參數 ACO 具有明顯差異。
其中，$\alpha$ 與 $\beta$ 皆有所提升，代表最佳化後的 ACO 在搜尋過程中更重視費洛蒙累積與啟發式資訊；
而 $\rho$ 則由 0.50 降低至 0.177945，表示費洛蒙揮發速度降低，使較佳路徑能在搜尋過程中保留較長時間。
$diffusion\_rad$ 由 2.0 收斂至 1.27，顯示在狹窄地形中，過大的費洛蒙擴散反而會造成其他管線的誤判與干擾，
GA 成功學習到了『適度收斂』的空間緩衝策略。
此外，$turn\_w$ 與 $clear\_w$ 的提升顯示 GA-ACO 在成本評估中更加重視轉彎懲罰與安全距離限制。

在該組 GA-ACO 參數下，實驗可得到最佳結果，其 Score 為 167.512。
此結果顯示 GA 能有效搜尋出適合 ACO 的參數組合，作為後續多地圖實驗比較的基礎。

\subsubsection{Performance Comparison}

為進一步驗證 GA-ACO 在不同地圖環境中的表現，本研究針對 Map0 至 Map4 進行固定參數 ACO 與 GA-ACO 的比較。每組地圖皆執行 20 次獨立測試，並計算其平均 Score。由於 Score 綜合考量路徑長度、轉彎次數、安全距離懲罰與重疊懲罰，因此數值越低代表管線佈局結果越佳。實驗結果如表 \ref{tab:aco_gaaco_comparison} 所示。

\begin{table}[htbp]
\caption{Average Score Comparison Between ACO and GA-ACO}
\label{tab:aco_gaaco_comparison}
\centering
\begin{tabular}{c c c c}
\hline
Map & ACO Avg. & GA-ACO Avg. & Improvement \\
\hline
Map0 & 23622.69 & 17996.00 & 23.8\% \\
Map1 & 286156.00 & 89714.23 & 68.6\% \\
Map2 & 397.74 & 113.30 & 71.5\% \\
Map3 & 776.74 & 149.56 & 80.7\% \\
Map4 & 807.07 & 215.98 & 73.2\% \\
\hline
\end{tabular}
\end{table}

由表 \ref{tab:aco_gaaco_comparison} 可觀察到，GA-ACO 在五組測試地圖上皆能取得低於固定參數 ACO 的平均 Score，表示 GA 自動調參能有效改善 ACO 的路徑搜尋品質。

在 Map0 中，GA-ACO 將平均 Score 由 23622.69 降低至 17996.00，改善幅度為 23.8\%。相較於其他地圖，Map0 的改善幅度較小，可能原因為其障礙物配置較規律，使固定參數 ACO 本身仍具有一定搜尋能力，因此 GA-ACO 的優勢較不明顯。

然而，當地圖複雜度提升後，GA-ACO 的改善效果更加顯著。在 Map1 中，平均 Score 由 286156.00 降低至 89714.23，改善幅度達 68.6\%。Map2、Map3 與 Map4 則分別達到 71.5\%、80.7\% 與 73.2\% 的改善幅度。其中 Map3 的改善幅度最高，代表在路徑選擇較複雜的情況下，GA 所搜尋出的參數組合能有效引導 ACO 避免陷入較差的局部解，並提升整體管線佈局品質。

整體而言，固定參數 ACO 在不同地圖中容易受到參數設定影響。若參數不適合目前地圖，可能導致演算法收斂速度過慢、陷入局部最佳解，或產生較高的安全距離與重疊懲罰。相較之下，GA-ACO 能透過演化機制自動搜尋較佳參數，使 ACO 在不同環境中取得更穩定且較低成本的解。

\subsubsection{Statistical Evaluation over 20 Independent Runs}


除了平均 Score 之外，本研究亦進一步分析 20 次獨立執行下的結果分布，以驗證 GA-ACO 的穩定性。固定參數 ACO 與 GA-ACO 在 Map0 至 Map4 上的盒鬚圖比較如圖 \ref{fig:boxplots_map01} 至圖 \ref{fig:boxplots_map4} 所示。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.47\columnwidth]{data/map0_boxplot.png}
\includegraphics[width=0.47\columnwidth]{data/map1_boxplot.png}
\caption{Boxplot comparison between ACO and GA-ACO over 20 independent runs on Map0 and Map1.}
\label{fig:boxplots_map01}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.47\columnwidth]{images/map0_dicuss0.png}
\includegraphics[width=0.47\columnwidth]{images/map0_dicuss1.png}
\caption{Map0 GA-ACO path visualization.}
\label{fig:vis_map01}
\end{figure}



由圖 \ref{fig:boxplots_map01} 可觀察到，
在 Map0 與 Map1 中，GA-ACO 的 Score 分布普遍低於固定參數 ACO。
Map0 中，GA-ACO 雖然仍存在一定變異，但整體分布較固定參數 ACO 低，
表示 GA 調參後能有效降低成本。Map0 的兩種方法皆呈現較大變異，
顯示該地圖本身具有較高搜尋難度，在Map0的20次參數驗證測試中，我們也發現儘管使用最佳參數
在測試中依舊會出現路徑交叉或是繞遠路的狀況，分析後推測是雙方最短路徑的成本相近，
導致 ACO 在搜尋過程中容易在兩條路徑間來回切換，造成結果變異較大。
相較之下，在 Map1 中，GA-ACO 的分布明顯集中於較低分數區間，
且變異明顯縮小，顯示 GA 調參後能使 ACO 在複雜地圖中更穩定地收斂至高品質解。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.47\columnwidth]{data/map2_boxplot.png}
\includegraphics[width=0.47\columnwidth]{data/map3_boxplot.png}
\caption{Boxplot comparison between ACO and GA-ACO over 20 independent runs on Map2 and Map3.}
\label{fig:boxplots_map23}
\end{figure}

由圖 \ref{fig:boxplots_map23} 可觀察到，Map2 與 Map3 的結果顯示，GA-ACO 不僅能降低 Score，也能縮小盒鬚圖的分布範圍，表示其在多次執行下具有較穩定的收斂表現。尤其在 Map3 中，GA-ACO 的結果集中程度明顯高於固定參數 ACO，代表最佳化後的參數組合能使演算法更穩定地收斂至相近品質的解。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.65\columnwidth]{data/map4_boxplot.png}
\caption{Boxplot comparison between ACO and GA-ACO over 20 independent runs on Map4.}
\label{fig:boxplots_map4}
\end{figure}

由圖 \ref{fig:boxplots_map4} 可觀察到，在 Map4 中，兩種方法的差異最為明顯。固定參數 ACO 的 Score 維持在較高區間，而 GA-ACO 幾乎穩定收斂於較低分數附近，顯示在障礙物配置較複雜、路徑選擇較受限制的情況下，GA-ACO 仍能維持良好的搜尋能力與穩定性。

綜合上述結果可知，GA-ACO 不僅能降低平均路徑成本，也能在多數地圖中減少不同執行結果之間的波動。此結果證明，利用基因演算法自動調整 ACO 超參數，能有效提升多管線佈局問題中的求解品質與穩定性。

\section{Conclusion}
本研究提出一套結合基因演算法與蟻群演算法之 GA-ACO 管線佈局最佳化架構，透過 GA 自動搜尋 ACO 的關鍵超參數，以降低傳統 ACO 對人工參數調整的依賴，並提升其在多管線路由問題中的求解能力。

實驗結果顯示，GA 所搜尋出的參數組合能有效改善 ACO 的搜尋效能。在五組測試地圖中，GA-ACO 皆能取得低於固定參數 ACO 的路徑成本，其中改善幅度最高可達 80.7\%。此外，透過 20 次獨立實驗之統計分析可發現，GA-ACO 不僅能獲得較低的平均 Score，其結果分布亦較為集中，顯示最佳化後的參數組合能有效提升演算法的穩定性與收斂品質。

綜合上述結果可知，利用基因演算法進行 ACO 參數最佳化，能有效提升多管線佈局問題之求解品質，並改善固定參數設定所造成的效能限制。未來研究可進一步考慮更大規模之地圖環境、更多種類之管線需求，以及動態障礙物或多目標最佳化等實際工程限制，以提升本方法於實際管線配置問題中的應用價值。


\begin{thebibliography}{00}
\bibitem{b1} W. Jian, Z. Xiaodong, and H. Yisha, ``A novel heuristic approach for 3D pipe routing in complex environments,'' \textit{IEEE Access}, vol. 6, pp. 49764-49775, 2018.
\bibitem{b2} 陳俊仁、翁世光, ``Pipe Routing Method Based on Ant Colony Algorithm'' \textit{國立臺灣海洋大學資訊工程學系碩士學位論文}, 2023.
\bibitem{b3} E. W. Dijkstra, ``A note on two problems in connexion with graphs,'' \textit{Numerische Mathematik}, vol. 1, pp. 269--271, 1959.
\bibitem{b4} P. E. Hart, N. J. Nilsson, and B. Raphael, ``A formal basis for the heuristic determination of minimum cost paths,'' \textit{IEEE Transactions on Systems Science and Cybernetics}, vol. 4, no. 2, pp. 100--107, 1968.
\bibitem{b5} D. E. Goldberg, \textit{Genetic Algorithms in Search, Optimization, and Machine Learning}. Reading, MA: Addison-Wesley, 1989.
\bibitem{b6} M. Dorigo, V. Maniezzo, and A. Colorni, ``Ant system: optimization by a colony of cooperating agents,'' \textit{IEEE Transactions on Systems, Man, and Cybernetics, Part B (Cybernetics)}, vol. 26, no. 1, pp. 29--41, Feb. 1996.
\bibitem{b7} J. M. Cecilia, J. M. García, A. Nisbet, D. Talia, and M. Amos, ``Enhancing data parallelism for Ant Colony Optimization on GPUs,'' \textit{Journal of Parallel and Distributed Computing}, vol. 73, no. 1, pp. 42--51, 2013.
\end{thebibliography}

\end{document}